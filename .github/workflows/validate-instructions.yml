name: validate-instructions

on:
  push:
  pull_request:

jobs:
  check-prose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run instruction prose guard
        run: |
          chmod +x scripts/check_instructions.sh
          scripts/check_instructions.sh
